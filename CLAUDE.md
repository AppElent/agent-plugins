# Agent plugin marketplace

This repository is the source of truth for the AppElent `mobile`, `web`, and
`workflow` plugins. Read a plugin's README before changing its public routing.

Run `pnpm check` before committing. A skill or command change requires a
version bump in all three manifests for that plugin. Package versions remain
independent from plugin versions.
