---
description: List, inspect, apply, or maintain an AppElent web feature
argument-hint: "[list | show <feature> | apply <feature> | capture <topic>]"
---

Load the `web-feature` skill and follow it with `$ARGUMENTS` as the requested
operation. Resolve catalog paths from this plugin and the repository root.
