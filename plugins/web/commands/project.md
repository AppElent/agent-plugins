---
description: Inspect AppElent web features used by the current project
argument-hint: "[list | status [--all] | issue <text> | issues | fix <n>]"
---

Load the `web-project` skill and follow it with `$ARGUMENTS` as the requested
operation. Preserve normal user authorization and provider safeguards.
