---
description: Route mobile product, React Native, Expo, QA, audit, and release work
argument-hint: "[list | show <skill> | request]"
---

Load the `mobile` skill and follow its routing instructions. Treat `$ARGUMENTS`
as the user's request. If no arguments are supplied, list the mobile plugin's
capabilities and the situations that trigger each one.
