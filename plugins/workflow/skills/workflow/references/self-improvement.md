# Workflow self-improvement

Capture only lessons that generalize beyond the current repository. Update the
smallest existing skill that owns the behavior; create a new skill only when no
existing owner has a matching trigger. Keep provider-neutral behavior in the
skill body and provider-specific mechanics in an explicitly labelled branch.

Any skill or command change requires the workflow plugin version to be bumped
in all three manifests and the repository validation suite to pass.
