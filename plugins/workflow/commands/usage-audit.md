---
description: Audit how this app behaves against usage-metered quotas — Convex, Cloudflare. Ranks by what scales, not by today's usage.
argument-hint: "help | list | [convex | cloudflare | <path or area>]"
---

Use the `usage-audit` skill from this plugin to handle: $ARGUMENTS

If no arguments were given, run the full audit across every provider in use.
`list` reports the providers, meters and check classes this skill covers,
without auditing anything.
