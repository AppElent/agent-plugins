---
description: Drive the app end-to-end in the browser, find bugs autonomously, and file one triaged GitHub issue.
argument-hint: "help | <scope, e.g. dark mode | mobile | what we just built>"
---

Use the `review-app` skill from this plugin to handle: $ARGUMENTS

If no arguments were given, review the whole app.
