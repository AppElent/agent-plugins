---
description: Upgrade Expo or React Native dependencies as a compatible version set, with an explicit patch/minor-only or include-majors scope.
argument-hint: "patch-minor | include-majors | <package or ecosystem to limit the upgrade to>"
---

Use the `upgrade-deps-mobile` skill from this plugin to handle: $ARGUMENTS

If the arguments do not select a scope, offer **Patch and minor** and
**Include majors** before changing dependencies. Do not include majors by
default.
