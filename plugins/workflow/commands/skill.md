---
description: Route to an AppElent engineering workflow
argument-hint: "[list | show <skill> | capture <topic> | request]"
---

Load the `workflow` skill and follow its routing instructions. Treat
`$ARGUMENTS` as the user's request. If no arguments are supplied, list the
workflow plugin's capabilities and when each should be used.
