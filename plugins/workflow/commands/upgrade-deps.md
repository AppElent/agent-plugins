---
description: Upgrade this app's dependencies safely — TanStack first, verify typecheck/lint/tests/build, then commit.
argument-hint: "help | <package or ecosystem to limit the upgrade to>"
---

Use the `upgrade-deps` skill from this plugin to handle: $ARGUMENTS

If no arguments were given, upgrade everything that is outdated.
